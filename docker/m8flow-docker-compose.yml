name: m8flow

services:
  m8flow-db:
    image: postgres:15
    env_file: ../.env
    ports:
      - "1111:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  keycloak-db:
    image: postgres:15
    env_file: ../.env
    environment:
      POSTGRES_DB: ${KEYCLOAK_DB_NAME:-keycloak}
      POSTGRES_USER: ${KEYCLOAK_DB_USER:-keycloak}
      POSTGRES_PASSWORD: ${KEYCLOAK_DB_PASSWORD:-keycloak}
    volumes:
      - keycloak-db-data:/var/lib/postgresql/data

  keycloak:
    build:
      context: ..
      dockerfile: docker/m8flow.keycloak.Dockerfile
    env_file: ../.env
    command: ["start-dev"]
    environment:
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN:-admin}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/${KEYCLOAK_DB_NAME:-keycloak}
      KC_DB_USERNAME: ${KEYCLOAK_DB_USER:-keycloak}
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD:-keycloak}
      KC_HTTP_PORT: ${KC_HTTP_PORT:-8080}
      KC_HOSTNAME_STRICT: ${KC_HOSTNAME_STRICT:-false}
      KC_HOSTNAME_STRICT_HTTPS: "false"
    ports:
      - "7002:8080"
    depends_on:
      - keycloak-db

  redis:
    image: redis:6-alpine
    command: ["redis-server", "--save", "20", "1", "--loglevel", "warning"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  minio:
    image: minio/minio:RELEASE.2025-01-20T14-49-07Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # -------------------------
  # INIT JOBS (opt-in)
  # -------------------------

  minio-mc-init:
    profiles: ["init"]
    image: minio/mc:RELEASE.2025-02-21T16-00-46Z
    depends_on:
      - minio
    entrypoint: ["/bin/sh", "/minio_mc_init.sh"]
    volumes:
      - ../extensions/m8flow-backend/bin/minio_mc_init.sh:/minio_mc_init.sh:ro
    restart: "no"

  process-models-sync:
    profiles: ["init"]
    image: rclone/rclone:1.66.0
    depends_on:
      - minio-mc-init
    volumes:
      - process_models_cache:/cache
      - ../rclone.conf:/config/rclone/rclone.conf:ro
      - ../extensions/m8flow-backend/bin/process_models_sync.sh:/process_models_sync.sh:ro
    entrypoint: ["/bin/sh", "/process_models_sync.sh"]
    restart: "no"

  templates-sync:
    profiles: ["init"]
    image: rclone/rclone:1.66.0
    depends_on:
      - minio-mc-init
    volumes:
      - templates_cache:/cache
      - ../rclone.conf:/config/rclone/rclone.conf:ro
      - ../extensions/m8flow-backend/bin/templates_sync.sh:/templates_sync.sh:ro
    entrypoint: ["/bin/sh", "/templates_sync.sh"]
    restart: "no"

  smtp-attachments-sync:
    profiles: ["init"]
    image: alpine:3.20
    entrypoint: ["/bin/sh", "/smtp_attachments_sync.sh"]
    volumes:
      - smtp_attachments:/app/email_attachments
      - ../email_attachments:/host_attachments:ro
      - ../extensions/m8flow-backend/bin/smtp_attachments_sync.sh:/smtp_attachments_sync.sh:ro
    restart: "no"

  # -------------------------
  # APP SERVICES
  # -------------------------

  m8flow-backend:
    build:
      context: ..
      dockerfile: docker/m8flow.backend.Dockerfile
    command: ["./extensions/m8flow-backend/bin/run_m8flow_backend.sh"]
    env_file: ../.env
    ports:
      - "8000:8000"
    volumes:
      - process_models_cache:/app/process_models
      - templates_cache:/app/templates
    depends_on:
      - m8flow-db
      - keycloak
      - redis
      - minio

  m8flow-frontend:
    build:
      context: ..
      dockerfile: docker/m8flow.frontend.Dockerfile
    env_file: ../.env
    ports:
      - "8001:80"
    depends_on:
      - m8flow-backend

  m8flow-connector-proxy:
    container_name: m8flow-connector-proxy
    image: m8flow/connector-proxy
    build:
      context: ../connector-proxy-demo
    environment:
      FLASK_ENV: "${FLASK_ENV:-development}"
      FLASK_DEBUG: "0"
      FLASK_SESSION_SECRET_KEY: "${FLASK_SESSION_SECRET_KEY:-super_secret_key}"
      CONNECTOR_PROXY_PORT: "${M8FLOW_CONNECTOR_PORT:-8004}"
      M8FLOW_CONNECTOR_SMTP_ATTACHMENTS_DIR: "${M8FLOW_CONNECTOR_SMTP_ATTACHMENTS_DIR:-./email_attachments}"
    ports:
      - "${M8FLOW_CONNECTOR_PORT:-8004}:${M8FLOW_CONNECTOR_PORT:-8004}/tcp"
    volumes:
      - type: volume
        source: "${M8FLOW_CONNECTOR_SMTP_ATTACHMENTS_VOLUME:-smtp_attachments}"
        target: /app/email_attachments
        read_only: true
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:${M8FLOW_CONNECTOR_PORT:-8004}/liveness || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

volumes:
  db-data:
    driver: local
  keycloak-db-data:
    driver: local
  redis-data:
    driver: local
  process_models_cache:
    driver: local
  templates_cache:
    driver: local
  minio_data:
    driver: local
  smtp_attachments:
    driver: local