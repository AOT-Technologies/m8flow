/**
 * Process instance show — delegate to upstream.
 *
 * Former m8flow fork only differed in getInfoTag() layout (M8F-370 full-width
 * grids via m8f-*-grid CSS). That polish is inessential relative to a 2k-line
 * LGPL-derived body; URI coverage remains via hooks/UriListForPermissions.
 */
export { default } from '@spiff-core/views/ProcessInstanceShow';
