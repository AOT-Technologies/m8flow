name: m8flow-keycloak CI/CD

on:
  push:
    branches:
      - develop
    paths:
      - "m8flow-keycloak/**"
      - "keycloak-extensions/**"
      - "docker/m8flow.keycloak.Dockerfile"
      - ".github/workflows/keycloak-cicd.yaml"

  pull_request:
    branches:
      - develop
    paths:
      - "m8flow-keycloak/**"
      - "keycloak-extensions/**"
      - "docker/m8flow.keycloak.Dockerfile"
      - ".github/workflows/keycloak-cicd.yaml"

  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "demo"
        type: choice
        options:
          - demo
          - qa
      image_uri:
        description: "Image URI to deploy (e.g. m8flow/m8flow-keycloak:1.0.6-rc). Leave empty to trigger a fresh build."
        required: false
        type: string

permissions:
  contents: read
  security-events: write
  actions: write
  id-token: write
  packages: write
  pull-requests: write
  deployments: write

concurrency:
  group: keycloak-cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ci:
    name: Keycloak CI Pipeline
    if: github.event_name != 'workflow_dispatch' || github.event.inputs.image_uri == ''
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/ci-orchestrator.yaml@v1
    with:
      config_path: keycloak-extensions/devx-ci.yaml
      auto_merge: false
      push: >-
        ${{
          (github.event_name == 'push' && github.ref == 'refs/heads/develop') ||
          github.event_name == 'workflow_dispatch'
        }}
    secrets:
      registry_username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry_password: ${{ secrets.DOCKERHUB_TOKEN }}
      sonar_token: ${{ secrets.SONAR_TOKEN }}

  cd-dev:
    name: Deploy Keycloak → DEV
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'push' && github.ref == 'refs/heads/develop'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: dev
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit

  cd-demo:
    name: Deploy Keycloak → DEMO
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.environment == 'demo'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: demo
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit

  cd-qa:
    name: Deploy Keycloak → QA
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.environment == 'qa'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: qa
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: keycloak-extensions/devx-config.yaml
    secrets: inherit
