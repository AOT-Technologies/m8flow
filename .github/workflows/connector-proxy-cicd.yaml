name: m8flow-connector-proxy CI/CD

on:
  push:
    branches:
      - develop
    paths:
      - "m8flow-connector-proxy/**"
      - ".github/workflows/connector-proxy-cicd.yaml"

  pull_request:
    branches:
      - develop
    paths:
      - "m8flow-connector-proxy/**"
      - ".github/workflows/connector-proxy-cicd.yaml"

  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "demo"
        type: choice
        options:
          - demo
          - qa
      image_uri:
        description: "Image URI to deploy (e.g. m8flow/m8flow-connector-proxy:1.0.6-rc). Leave empty to trigger a fresh build."
        required: false
        type: string

permissions:
  contents: read
  security-events: write
  actions: write
  id-token: write
  packages: write
  pull-requests: write
  deployments: write

concurrency:
  group: connector-proxy-cicd-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ci:
    name: Connector Proxy CI Pipeline
    if: github.event_name != 'workflow_dispatch' || github.event.inputs.image_uri == ''
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/ci-orchestrator.yaml@v1
    with:
      config_path: m8flow-connector-proxy/devx-ci.yaml
      auto_merge: false
      push: >-
        ${{
          (github.event_name == 'push' && github.ref == 'refs/heads/develop') ||
          github.event_name == 'workflow_dispatch'
        }}
    secrets:
      registry_username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry_password: ${{ secrets.DOCKERHUB_TOKEN }}

  cd-dev:
    name: Deploy Connector Proxy → DEV
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'push' && github.ref == 'refs/heads/develop'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: dev
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: m8flow-connector-proxy/devx-config.yaml
    secrets: inherit

  cd-demo:
    name: Deploy Connector Proxy → DEMO
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.environment == 'demo'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: demo
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: m8flow-connector-proxy/devx-config.yaml
    secrets: inherit

  cd-qa:
    name: Deploy Connector Proxy → QA
    needs: ci
    if: |
      always() &&
      (needs.ci.result == 'success' || needs.ci.result == 'skipped') &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.environment == 'qa'
    uses: AOT-Technologies/devx-reusable-workflows/.github/workflows/cd-orchestrator.yaml@v1
    with:
      environment: qa
      image_uri: ${{ github.event.inputs.image_uri || needs.ci.outputs.image_uri }}
      config_path: m8flow-connector-proxy/devx-config.yaml
    secrets: inherit
